# Core runtime -- installs cleanly on a laptop, no GPU required.
# For the full README tech stack (LangChain/CrewAI, ColPali/Docling,
# vLLM/TensorRT-LLM), see requirements-extras.txt.

fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6,<3
pydantic-settings>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9
httpx>=0.27

# Neuro-symbolic validation
z3-solver>=4.13

# Vision-native-ish PDF parsing (text + bounding boxes, page rasterization)
pymupdf>=1.24

# Graph + vector DB clients
neo4j>=5.19
qdrant-client>=1.10

# LLM SDKs (only used if LLM_PROVIDER=openai/anthropic; mock mode needs neither)
openai>=1.30
anthropic>=0.30

tenacity>=8.2

# Testing
pytest>=8.0
